apiVersion: v1
kind: Service
metadata:
  name: {{ include "goodface-user.fullname" . }}
  labels:
    {{- include "goodface-user.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 20882
      targetPort: dubbo
      protocol: TCP
      name: dubbo
    - port: 22222
      targetPort: qos
      protocol: TCP
      name: qos
  selector:
    {{- include "goodface-user.selectorLabels" . | nindent 4 }}